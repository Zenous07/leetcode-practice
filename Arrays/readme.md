# Arrays

## 📌 Overview
This folder contains solutions to **array-based problems** from LeetCode.
Each problem focuses on common array techniques such as traversal, prefix sums,
two pointers, greedy strategies, and sliding window approaches.

The goal of this folder is to build strong problem-solving skills using arrays,
which form the foundation of many data structures and algorithms.

---

## 🧠 Common Techniques Used
The problems in this folder primarily use the following techniques:

- One-pass traversal
- Prefix and suffix computation
- Two-pointer technique
- Sliding window
- Greedy approach
- Brute force (for comparison)
- Space optimization

---

## 📂 Folder Structure
Each problem is organized in its own directory using the following format:

